# Testcontainers will auto-configure the datasource via @ServiceConnection
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=test-secret-key-for-testing-purposes-only-256bits
jwt.expiration=86400000
